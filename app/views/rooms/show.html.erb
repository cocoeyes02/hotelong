<h2>部屋詳細</h2>
<table class="table">
  <tr>
    <th scope="row">部屋番号</th>
    <th scope="row">一人あたりの価格</th>
    <th scope="row">部屋の形式</th>
    <th scope="row">標準宿泊人数</th>
    <th scope="row">エキストラベット</th>
  </tr>
  <tr>
    <td><%= @room.room_number %></td>
    <td><%= @room.person_price %></td>
    <td><%= @room.style_name %></td>
    <td><%= @room.expect_count %>人</td>
    <td>
      <% if @room.can_add_bed? %>
        あり
      <% else %>
        なし
      <% end %>
    </td>
  </tr>
</table>
<table class="table">
  <tr>
    <th scope="row">指定できるプラン</th>
    <th scope="row">価格</th>
    <th scope="row">宿泊人数</th>
  </tr>
  <% @plans.each do |plan| %>
    <% # ノープランは表示しない
    if plan.id == 1
      next
    end
    %>
    <tr>
      <td><%= plan.name %></td>
      <td><%= plan.price %></td>
      <td><%= plan.apply_count %></td>
    </tr>
  <% end %>
</table>
<p><%= link_to 'この部屋に宿泊する', :new_reservation, class: "btn btn-primary" %></p>
<button type="button" class="btn btn-default" onclick="history.back()">戻る</button>